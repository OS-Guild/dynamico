{
  "name": "root",
  "private": true,
  "workspaces": [
    "examples/*",
    "client/*",
    "server/*"
  ],
  "scripts": {
    "react-demo": "lerna run start --scope=react-dynamico-example --parallel",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
