{
  "name": "@dynamico/core",
  "version": "0.0.1",
  "main": "./index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && npm run compile && npm run copy-package-json",
    "compile": "tsc",
    "copy-package-json": "cp ./package.json ./dist/package.json",
    "clean": "rm -rf ./dist"
  },
  "devDependencies": {
    "@types/compare-versions": "^3.0.0",
    "@types/deepmerge": "^2.2.0",
    "ts-lint": "^4.5.1",
    "typescript": "^3.3.3333"
  },
  "dependencies": {
    "build-url": "^1.3.2",
    "compare-versions": "^3.4.0",
    "deepmerge": "^3.2.0"
  }
}
